#include "tsqueue.h"

void threadsafe_queue_init(ThreadsafeQueue *q) {
    // TODO
    static_cast<void>(q);  // Используем переменную хоть как-то, чтобы не было предупреждения.
}

void threadsafe_queue_destroy(ThreadsafeQueue *q) {
    // TODO
    static_cast<void>(q);  // Используем переменную хоть как-то, чтобы не было предупреждения.
}

void threadsafe_queue_push(ThreadsafeQueue *q, void *data) {
    // TODO
    static_cast<void>(q);  // Используем переменную хоть как-то, чтобы не было предупреждения.
    static_cast<void>(data);  // Используем переменную хоть как-то, чтобы не было предупреждения.
}

void *threadsafe_queue_wait_and_pop(ThreadsafeQueue *q) {
    // TODO
    static_cast<void>(q);  // Используем переменную хоть как-то, чтобы не было предупреждения.
    return nullptr;
}
